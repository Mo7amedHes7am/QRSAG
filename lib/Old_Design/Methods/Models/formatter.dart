// class Formatter {
//   bool hasURLs(String text) {
//     final regex = RegExp(
//       r'^(https?|ftp)://[^\s/$.?#].[^\s]*$', // basic URL pattern
//     );
//     return (regex.hasMatch(text));
//   }
// }
